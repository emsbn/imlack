import React from 'react';
import Workspace from '@layouts/Workspace';

const Channel = () => {
  return (
    <Workspace>
      <div>채널 가입을 축하합니다.</div>
    </Workspace>
  );
};

export default Channel;
